spring.application.name=hr-worker

#to server config
spring.cloud.config.uri=http://hr-config-server:8888

spring.profiles.active=prod

# refresh configs in runtime
management.endpoints.web.exposure.include=*